#include <stdio.h>
/**
 *  main - prints 1 digit integers.
 *
 *  Return: 0 always.
 */

int main(void)
{
	int num;

	for (num = 0; num < 10; num++)
		printf("%d", num);

	printf("\n");

	return (0);
}
